[project]
name = "git-back"
version = "0.1.0"
description = "..."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pygithub>=2.9.1",
    "python-dotenv>=1.2.2",
]

[tool.ruff.format]
indent-style = "tab"

